<div class="medium-post">
  <h1>{{ page.title }}</h1>
  <p class="date">{{ page.date | date: "%Y-%m-%d" }}</p>
  <div class="content">
    {{ page.content }}
  </div>
  <a href="{{ page.url }}" target="_blank">View on Medium</a>
</div>